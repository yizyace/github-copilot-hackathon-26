import '../styles/theme.css'

// Placeholder — the full Soul editor is wired up next.
function Editor() {
  return (
    <div className="theme-soul" style={{ minHeight: '100vh', display: 'grid', placeItems: 'center' }}>
      <p>Soul editor — coming up.</p>
    </div>
  )
}

export default Editor
